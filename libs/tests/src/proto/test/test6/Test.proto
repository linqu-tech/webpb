syntax = "proto2";

package TestProto;

option java_package = "test";

import "webpb/WebpbExtend.proto";

option (f_opts).java = {
    import: 'lombok.ToString'
};

enum Test {
    option (e_opts).java = {
        annotation: '@ToString'
    };

    DEFAULT = 0;
}
